/*
    Idempotent
    It means function can be repeated many times and you'll still
    end up with the same result for same value.

    Everytime
    Same input ====> same output
*/


function multiplyByTwo (num) {
    return num * 2;
}