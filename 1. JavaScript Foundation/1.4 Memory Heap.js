const numb = 4;
const str = 'some string';
const obj = { name: 'Ashish' };


// so 4, 'some string', {name: 'Ashish'} are gets stored in memory heap

// memory heap is the randomly accessed memory






// how to fill memory heap

let arr = [];
for (let i = 5; i > 1; i++) {
    arr.push(i);
}